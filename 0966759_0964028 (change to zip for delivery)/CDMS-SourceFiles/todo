
- Check what needs to be done when username or password is entered wrong too many times

- User manual

ALWAYS TODO: ALL SQL EXECUTIONS MUST BE SQL INJECTION SAFE, SO EVERY TIME YOU PASS DATA, USE A PREPARED STATEMENT

##################################################################################################################

Menu:
+ Advisor 
    + Update password
    + Add client
    + mod client
    + search client

- System Admin
    + everything an advisor can do
    + delete client
    + Check list of users and their role
    + add Advisor
    + mod Advisor
    + del Advisor
    - reset Advisor password (temp pass)
    - backup System
    - see log
    
- Super Admin (main use is to define sys admins)
    - everything an sysadmin can do
    - add sysad
    - mod sysad
    - del sysad
    - reset sysad pass (temp pass)





#######################################################################################################
Log (check what kind of file log should be):
- Log all activities
- Flag suspicious activities:
    - Input other than predefined Input
    - ...
- encrypted and only readable through the UI, by an admin
- Produce alert to admin for unread suspicous activities

Encryption with Caesar cypher or vigenere cypher:
- Usernames
- Passwords
- Client phone number
- Client address
- Email
- Log

Backup in zip format:
- database
-- employee info
-- client info
- log



############################################################################################################
Usernames:
-   must have a length of at least 5 characters
-   must be no longer than 20 characters
-   must be started with a letter
-   can contain letters (a-z), numbers (0-9), dashes (-), underscores (_), apostrophes ('), and periods (.)
-   no distinguish between lowercase or uppercase letters

Password:
-   must have a length of at least 8 characters
-   must be no longer than 30 characters
-   can contain letters (a-z), (A-Z), numbers (0-9), Special characters such as ~!@#$%^&*_-+=`|\(){}[]:;'<>,.?/
-   must have a combination of at least one lowercase letter, one uppercase letter, one digit, and one special character

##################################################################################################################
C1, Authentication & Authorization      [in progress]:
C2, Input validation                    [in progress]:
C3, SQL Injection                       [in progress]:
C4, Invalid inputs                      [in progress]:
C5, Log & Backup                        [not started]: